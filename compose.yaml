name: iphone18-stockroom
services:
  stockroom:
    build: .
    ports:
      - "127.0.0.1:8765:8765"
    volumes:
      - stockroom-data:/data
    restart: unless-stopped
    init: true
    security_opt:
      - no-new-privileges:true
volumes:
  stockroom-data:
